import type {NextPage} from 'next'
import {dehydrate, QueryClient} from "react-query";

const Home: NextPage = () => {
    return (
        <div>
            Hello world!
        </div>
    )
}

export default Home
